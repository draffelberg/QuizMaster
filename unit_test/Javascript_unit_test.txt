
const game = require('./game');

test('All questions answered', () => {
  expect(stateHandle()).toBe(false);
})


test('All questions did not answered', () => {
    expect(stateHandle()).toBe(true);
  })
  

test('All radio buttons have been clicked', () => {
  expect(submitAnswers()).toBe(true);
})


test('All radio buttons have not been clicked', () => {
    expect(submitAnswers()).toBe(false);
  })
